{
  "name": "@prisma-next-idb/cli-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Regression tests for the `prisma-next-idb` CLI binary (migration plan/contract-space/preflight, config-driven path resolution). Drives the published binary via execa and asserts on stdout/stderr/exit codes/files.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@prisma-next-idb/family-idb": "workspace:*",
    "@prisma-next/migration-tools": "^0.16.0",
    "@types/node": "^24.10.9",
    "execa": "^10.0.1",
    "vitest": "^4.1.10"
  }
}
