{
  "name": "@prisma-next-idb/cli-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Regression tests for the `prisma-next-idb` CLI binary (generate-contract-space, preflight). Drives the published binary via execa and asserts on stdout/stderr/exit codes/files.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@prisma-next-idb/family-idb": "workspace:*",
    "@prisma-next/migration-tools": "^0.14.0",
    "@types/node": "^24.13.2",
    "execa": "^9.6.1",
    "vitest": "^4.1.9"
  }
}
