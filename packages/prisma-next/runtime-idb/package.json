{
  "name": "@prisma-next-idb/runtime-idb",
  "version": "0.0.1",
  "description": "Prisma Next IDB family — runtime (RuntimeCore subclass that wires adapter + driver)",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./runtime": "./dist/exports/runtime.mjs",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "check": "tsc --noEmit",
    "lint": "prettier --check . && eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@prisma-next/framework-components": "^0.4.4",
    "@prisma-next-idb/adapter-idb": "workspace:*",
    "@prisma-next-idb/driver-idb": "workspace:*"
  },
  "devDependencies": {
    "tsdown": "^0.22.0",
    "typescript": "^6.0.3"
  }
}
