{
  "scripts": {
    "lint": "pnpm run -r lint",
    "format": "pnpm run -r format",
    "test:client": "pnpm run --filter @prisma-idb/idb-client-generator build && pnpm run --filter usage test",
    "test:sync": "pnpm run --filter @prisma-idb/idb-client-generator build && pnpm run --filter usage-sync test",
    "test": "pnpm run --filter @prisma-idb/idb-client-generator build && pnpm run test:client && pnpm run test:sync",
    "dev": "pnpm run --filter usage dev",
    "check": "pnpm run --filter usage check",
    "generate": "pnpm run --filter @prisma-idb/idb-client-generator build && pnpm run --filter usage generate && pnpm run --filter usage-sync generate"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/prisma-idb/idb-client-generator.git",
    "directory": "packages/generator"
  }
}
